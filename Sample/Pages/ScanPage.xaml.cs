namespace Sample.Pages;

// All logic lives in ScanViewModel (assigned as BindingContext by Shiny Shell).
public partial class ScanPage : ContentPage
{
    public ScanPage() => this.InitializeComponent();
}
