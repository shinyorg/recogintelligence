<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fi="clr-namespace:Shiny.FaceIntelligence.Maui;assembly=Shiny.FaceIntelligence.Maui"
             xmlns:shiny="clr-namespace:Shiny;assembly=Shiny.Maui.Shell"
             xmlns:vm="clr-namespace:Sample.Features.Face.Pages"
             x:Class="Sample.Features.Face.Pages.RecognizePage"
             x:DataType="vm:RecognizeViewModel"
             Title="Recognize">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Home"
                     shiny:Navigate.Route="Home"
                     shiny:Navigate.RelativeNavigation="False" />
    </ContentPage.ToolbarItems>

    <Grid>
        <!-- The whole recognition pipeline: preview, permission, detection, matching. The control resolves
             the analyzer from DI itself, so there is nothing to wire up here. -->
        <fi:FaceRecognitionView x:Name="FaceCamera"
                           FaceRecognized="OnFaceRecognized"
                           CameraFailed="OnCameraFailed" />

        <Border VerticalOptions="End"
                Margin="16"
                Padding="16,12"
                BackgroundColor="#CC000000"
                StrokeShape="RoundRectangle 14">
            <VerticalStackLayout Spacing="4">
                <Label Text="{Binding ResultText}"
                       TextColor="White"
                       FontSize="18"
                       HorizontalTextAlignment="Center" />
                <Label Text="{Binding DiagnosticText}"
                       TextColor="#99FFFFFF"
                       FontSize="11"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Border>
    </Grid>

</ContentPage>
