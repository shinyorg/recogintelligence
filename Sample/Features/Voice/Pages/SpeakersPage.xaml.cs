namespace Sample.Features.Voice.Pages;

// All logic lives in SpeakersViewModel (assigned as BindingContext by Shiny Shell).
public partial class SpeakersPage : ContentPage
{
    public SpeakersPage() => this.InitializeComponent();
}
